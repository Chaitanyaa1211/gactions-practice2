name: simple commands task

on:
  push:

jobs:

  commands:
    runs-on: ubuntu-alatesttt

    steps:
      - name: simple command task

        run: |
          pwd
          ls -la
          whoami
          uname -a
