name: simple commands task

on:
  push:

jobs:

  commands:
    runs-on: ubuntu-latest

    steps:
      - name: simple command task
      - uses: actions/checkout@4

        run: |
          docker --version
          git --version
          kubectl version --client
