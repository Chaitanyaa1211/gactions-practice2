name: simple commands task

on:
  push:

jobs:

  commands:
    runs-on: ubuntu-latest

    steps:
      - name: simple command task

        run: |
          docker --version
          git --version
          kubectl version --client
